[project]
name = "tiny-inference"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.10.0",
    "transformers>=5.3.0",
    "matplotlib>=3.8.0",
    "torchvision>=0.25.0",
]
